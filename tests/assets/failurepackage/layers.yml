High level:
  containers:
    - failurepackage
  layers:
    - three
    - two
    - one

Modular:
  containers:
    - failurepackage.one
    - failurepackage.two
    - failurepackage.three
  layers:
    - gamma
    - beta
    - alpha
