High level:
  packages:
    - failurepackage
  layers:
    - three
    - two
    - one

Modular:
  packages:
    - failurepackage.one
    - failurepackage.two
    - failurepackage.three
  layers:
    - gamma
    - beta
    - alpha
