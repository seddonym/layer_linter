Contract A:
  containers:
    - singlecontractfile.foo
    - singlecontractfile.bar
  layers:
    - one
    - two

Contract B:
  containers:
    - singlecontractfile
  layers:
    - one
    - two
    - three
  whitelisted_paths:
    - baz.utils <- baz.three.green
    - baz.three.blue <- baz.two
