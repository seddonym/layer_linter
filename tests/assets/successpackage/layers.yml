High level:
  containers:
    - successpackage
  layers:
    - three
    - two
    - one
  whitelisted_paths:
    - successpackage.utils <- successpackage.three.alpha

Modular:
  containers:
    - successpackage.one
    - successpackage.two
    - successpackage.three
  layers:
    - (delta)
    - gamma
    - beta
    - alpha
