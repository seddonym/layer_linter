Layer Linter architecture:
  containers:
    - layer_linter
  layers:
    - cmdline
    - reports
    - contract
    - dependencies
    - module

Dependencies subpackage:
    containers:
        - layer_linter.dependencies
    layers:
        - graph
        - analysis
        - scanner
        - path
