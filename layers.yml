Layer Linter architecture:
  containers:
    - layer_linter
  layers:
    - cmdline
    - report
    - contract
    - dependencies
    - module

Dependencies subpackage:
    containers:
        - layer_linter.dependencies
    layers:
        - graph
        - analysis
        - scanner
        - path
