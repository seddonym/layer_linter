Layer Linter architecture:
  packages:
    - layer_linter
  layers:
    - module
    - reports
    - cmdline
    - contract
    - dependencies

Dependencies subpackage:
    packages:
        - layer_linter.dependencies
    layers:
        - graph
        - analysis
        - scanner
        - path
