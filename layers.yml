Layer Linter architecture:
  packages:
    - layer_linter
  layers:
    - cmdline
    - reports
    - contract
    - dependencies
    - module

Dependencies subpackage:
    packages:
        - layer_linter.dependencies
    layers:
        - graph
        - analysis
        - scanner
        - path
